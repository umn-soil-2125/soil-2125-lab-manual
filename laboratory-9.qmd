# **Soil Classification**

::: callout-note
## Objectives

-   Identify soil epipedons, subsurface diagnostic horizons.
-   Locate the 12 soil orders in the United States.
-   Break down a taxonomic soil name.
:::

## INVESTIGATION A: Soil Classification

**READ THESE INSTRUCTIONS COMPLETELY!**

For all eight monoliths, determine the correct order of horizons from the choices given, epipedon, diagnostic subsurface horizon, and soil order/suborder. All monoliths are from Minnesota, except for the Cecil, which is from North Carolina. The most important take-aways are horizon nomenclatures and classifying a soil based on morphology. Characteristics are given to assist you with your classifications -- use your 1 page cheat sheet!

**Note**: Mollic epipedons are darker than 3/3 (no matter what the hue) and must be at least 25cm (\~10 inches) thick. If the epipedon is not mollic, it must be ochric or histic. None of these soils have an umbric epipedon. Bw = cambic (except for Bw that have sand/loamy sand textures -- these are not considered as any diagnostic horizon for the purposes of classification); Bt = argillic; Bs, Bhs, Bhsm, etc. = spodic; Bo = oxic

## SOIL 1: CROMWELL

HORIZON CHOICES: Bw1, A, Bw2, BA, 2C

NOTES: B.S. = 62%

```{r, echo = FALSE, message = FALSE, warning = FALSE}
library(huxtable)
library(dplyr)

################
## BUILD TABLE
################
Horizon <- c("", "", "", "", "")
Texture <- c("Loam", "Loam", "Loam", "Loam", "Sand")
Color <- c("7.5YR 2/2", "7.5YR 3/4", "7.5YR 4/4", "7.5YR 4/4", "7.5YR 5/4")
Depth <- c("0-13 cm", "13-28 cm", "28-36 cm", "36-51 cm", "51+ cm")

cromwellhorizon <- data.frame(Horizon, Texture, Color, Depth)
colnames(cromwellhorizon) = c("Horizon", "Texture", "Color", "Depth")

col.num = 4
row.num = 6 #including header

################
## SET AS HUXTABLE TYPE
################
cromwellhorizon_ht <- as_hux(cromwellhorizon)

################
## SET FORMATTING
################
#NOTE: only thing you really need to mess with down here is the padding. If you need extra row height, keep them - if not, just put a hastag in front of them

tab1.1 = cromwellhorizon_ht %>% 
      set_all_padding(4) %>% 
      set_outer_padding(0) %>% 
      set_number_format(0) %>% #this tells it how many decimals to print after any numbers
      set_bold(row = 1, col = everywhere) %>% #make header row bold
      #set_bottom_border(row = 1, col = everywhere) %>% # probably dont need this because you set all borders below
      set_width(0.9) %>% #; 1 == full width
      set_align(row = 1:row.num, col = 1:col.num, value = 'center') %>%
      set_valign(row = 1:row.num, col = 1:col.num, value = 'middle') %>%
      #set_number_format(2:3, 1, value = NA) %>% #; don't format these cells as numbers
      #set_col_width(c(0.3, 0.6)) %>%
      set_latex_float(value = "h!") %>%
      #set_row_height(1:3, c(0.02, 0.2, 0.2)) %>%
      set_bottom_padding(row = 1:6, col= 1:4, 12) %>%
      set_top_padding(row = 1:6, col = 1:4, 12) %>%
      #set_row_height(c(0.02, 0.2, 0.2)) %>%
      set_all_borders(brdr(1, "solid", "black"))
      #set_caption("Pots of jam for sale") # this is if we want a caption - I don't think we need any captions but leaving it here just in case

tab1.1
```

Epipedon:                                                                     Explain:

Diagnostic Subsurface:                                              Explain:

Soil Order:                                                                    Explain:

Soil Suborder:                                                              Explain:

## SOIL 2: WEBSTER

HORIZON CHOICES: Cg, Ap, AC, A2, A1

NOTES: BS = 68%

```{r, echo = FALSE, message = FALSE, warning = FALSE}
library(huxtable)
library(dplyr)

################
## BUILD TABLE
################
Horizon <- c("", "", "", "", "")
Texture <- c("Silty Clay Loam", "Silty Clay Loam", "Clay Loam", "Clay Loam", "Clay Loam")
Color <- c("10YR 2/1", "10YR 2/1", "10YR 3/2", "5Y 4/1", "5Y 6/2")
Depth <- c("0-15 cm", "15-40 cm", "40-69 cm", "69-89 cm", "89+ cm")

websterhorizon <- data.frame(Horizon, Texture, Color, Depth)
colnames(websterhorizon) = c("Horizon", "Texture", "Color", "Depth")

col.num = 4
row.num = 6 #including header

################
## SET AS HUXTABLE TYPE
################
websterhorizon_ht <- as_hux(websterhorizon)

################
## SET FORMATTING
################
#NOTE: only thing you really need to mess with down here is the padding. If you need extra row height, keep them - if not, just put a hastag in front of them

tab1.1 = websterhorizon_ht %>% 
      set_all_padding(4) %>% 
      set_outer_padding(0) %>% 
      set_number_format(0) %>% #this tells it how many decimals to print after any numbers
      set_bold(row = 1, col = everywhere) %>% #make header row bold
      #set_bottom_border(row = 1, col = everywhere) %>% # probably dont need this because you set all borders below
      set_width(0.9) %>% #; 1 == full width
      set_align(row = 1:row.num, col = 1:col.num, value = 'center') %>%
      set_valign(row = 1:row.num, col = 1:col.num, value = 'middle') %>%
      #set_number_format(2:3, 1, value = NA) %>% #; don't format these cells as numbers
      #set_col_width(c(0.3, 0.6)) %>%
      set_latex_float(value = "h!") %>%
      #set_row_height(1:3, c(0.02, 0.2, 0.2)) %>%
      set_bottom_padding(row = 1:6, col= 1:4, 12) %>%
      set_top_padding(row = 1:6, col = 1:4, 12) %>%
      #set_row_height(c(0.02, 0.2, 0.2)) %>%
      set_all_borders(brdr(1, "solid", "black"))
      #set_caption("Pots of jam for sale") # this is if we want a caption - I don't think we need any captions but leaving it here just in case

tab1.1
```

Epipedon:                                                                     Explain:

Diagnostic Subsurface:                                              Explain:

Soil Order:                                                                    Explain:

Soil Suborder:                                                              Explain:

## SOIL 3: RIFLE

HORIZON CHOICES: Oa, Oe2, Oe1, Oe3, 2C

```{r, echo = FALSE, message = FALSE, warning = FALSE}
library(huxtable)
library(dplyr)

################
## BUILD TABLE
################
Horizon <- c("", "", "", "", "")
Texture <- c("Mucky-peat", "Mucky-peat", "Mucky-peat", "Muck", "Loam")
Color <- c("5YR 2/2", "5YR 2/2", "5YR 2/2", "5YR 2/1", "2.5Y 5/1")
Depth <- c("0-20 cm", "20-51 cm", "51-63 cm", "63-94 cm", "94+ cm")

riflehorizon <- data.frame(Horizon, Texture, Color, Depth)
colnames(riflehorizon) = c("Horizon", "Texture", "Color", "Depth")

col.num = 4
row.num = 6 #including header

################
## SET AS HUXTABLE TYPE
################
riflehorizon_ht <- as_hux(riflehorizon)

################
## SET FORMATTING
################
#NOTE: only thing you really need to mess with down here is the padding. If you need extra row height, keep them - if not, just put a hastag in front of them

tab1.1 = riflehorizon_ht %>% 
      set_all_padding(4) %>% 
      set_outer_padding(0) %>% 
      set_number_format(0) %>% #this tells it how many decimals to print after any numbers
      set_bold(row = 1, col = everywhere) %>% #make header row bold
      #set_bottom_border(row = 1, col = everywhere) %>% # probably dont need this because you set all borders below
      set_width(0.9) %>% #; 1 == full width
      set_align(row = 1:row.num, col = 1:col.num, value = 'center') %>%
      set_valign(row = 1:row.num, col = 1:col.num, value = 'middle') %>%
      #set_number_format(2:3, 1, value = NA) %>% #; don't format these cells as numbers
      #set_col_width(c(0.3, 0.6)) %>%
      set_latex_float(value = "h!") %>%
      #set_row_height(1:3, c(0.02, 0.2, 0.2)) %>%
      set_bottom_padding(row = 1:6, col= 1:4, 12) %>%
      set_top_padding(row = 1:6, col = 1:4, 12) %>%
      #set_row_height(c(0.02, 0.2, 0.2)) %>%
      set_all_borders(brdr(1, "solid", "black"))
      #set_caption("Pots of jam for sale") # this is if we want a caption - I don't think we need any captions but leaving it here just in case

tab1.1
```

Epipedon:                                                                     Explain:

Diagnostic Subsurface:                                              Explain:

Soil Order:                                                                    Explain:

Soil Suborder:                                                              Explain:

## SOIL 4: FARGO

HORIZON CHOICES: Cg, Bss, Bg1, Bg2, Bg3, Ap

NOTES: BS = 100%; large cracks open during the dry season; all horizons \> 55% clay

```{r, echo = FALSE, message = FALSE, warning = FALSE}
library(huxtable)
library(dplyr)

################
## BUILD TABLE
################
Horizon <- c("", "", "", "", "", "")
Texture <- c("Silty Clay", "Silty Clay", "Clay", "Clay", "Clay", "Clay")
Color <- c("10YR 3.1", "10YR 4/2", "2.5Y 3/1", "5Y 5/2", "2.5Y 5/2", "5Y 6/3")
Depth <- c("0-23 cm", "23-41 cm", "41-53 cm", "53-66 cm", "66-86 cm", "86+ cm")

fargohorizon <- data.frame(Horizon, Texture, Color, Depth)
colnames(fargohorizon) = c("Horizon", "Texture", "Color", "Depth")

col.num = 4
row.num = 7 #including header

################
## SET AS HUXTABLE TYPE
################
fargohorizon_ht <- as_hux(fargohorizon)

################
## SET FORMATTING
################
#NOTE: only thing you really need to mess with down here is the padding. If you need extra row height, keep them - if not, just put a hastag in front of them

tab1.1 = fargohorizon_ht %>% 
      set_all_padding(4) %>% 
      set_outer_padding(0) %>% 
      set_number_format(0) %>% #this tells it how many decimals to print after any numbers
      set_bold(row = 1, col = everywhere) %>% #make header row bold
      #set_bottom_border(row = 1, col = everywhere) %>% # probably dont need this because you set all borders below
      set_width(0.9) %>% #; 1 == full width
      set_align(row = 1:row.num, col = 1:col.num, value = 'center') %>%
      set_valign(row = 1:row.num, col = 1:col.num, value = 'middle') %>%
      #set_number_format(2:3, 1, value = NA) %>% #; don't format these cells as numbers
      #set_col_width(c(0.3, 0.6)) %>%
      set_latex_float(value = "h!") %>%
      #set_row_height(1:3, c(0.02, 0.2, 0.2)) %>%
      set_bottom_padding(row = 1:7, col= 1:4, 12) %>%
      set_top_padding(row = 1:7, col = 1:4, 12) %>%
      #set_row_height(c(0.02, 0.2, 0.2)) %>%
      set_all_borders(brdr(1, "solid", "black"))
      #set_caption("Pots of jam for sale") # this is if we want a caption - I don't think we need any captions but leaving it here just in case

tab1.1
```

Epipedon:                                                                     Explain:

Diagnostic Subsurface:                                              Explain:

Soil Order:                                                                    Explain:

Soil Suborder:                                                              Explain:

## SOIL 5: KALKASKA

HORIZON CHOICES: BC, A, E, Bhs, C, Bs

NOTES: BS = 42%

```{r, echo = FALSE, message = FALSE, warning = FALSE}
library(huxtable)
library(dplyr)

################
## BUILD TABLE
################
Horizon <- c("", "", "", "", "", "")
Texture <- c("Loamy Sand", "Loamy Sand", "Loamy Sand", "Loamy Sand", "Sand", "Sand")
Color <- c("7.5YR 2.5/1", "7.5YR 6/1", "7.5YR 4/4", "5YR 3/3", "10YR 5/6", "10YR 6/4")
Depth <- c("0-5 cm", "5-27 cm", "27-41 cm", "41-54 cm", "54-80 cm", "80+ cm")

kalkaskahorizon <- data.frame(Horizon, Texture, Color, Depth)
colnames(kalkaskahorizon) = c("Horizon", "Texture", "Color", "Depth")

col.num = 4
row.num = 7 #including header

################
## SET AS HUXTABLE TYPE
################
kalkaskahorizon_ht <- as_hux(kalkaskahorizon)

################
## SET FORMATTING
################
#NOTE: only thing you really need to mess with down here is the padding. If you need extra row height, keep them - if not, just put a hastag in front of them

tab1.1 = kalkaskahorizon_ht %>% 
      set_all_padding(4) %>% 
      set_outer_padding(0) %>% 
      set_number_format(0) %>% #this tells it how many decimals to print after any numbers
      set_bold(row = 1, col = everywhere) %>% #make header row bold
      #set_bottom_border(row = 1, col = everywhere) %>% # probably dont need this because you set all borders below
      set_width(0.9) %>% #; 1 == full width
      set_align(row = 1:row.num, col = 1:col.num, value = 'center') %>%
      set_valign(row = 1:row.num, col = 1:col.num, value = 'middle') %>%
      #set_number_format(2:3, 1, value = NA) %>% #; don't format these cells as numbers
      #set_col_width(c(0.3, 0.6)) %>%
      set_latex_float(value = "h!") %>%
      #set_row_height(1:3, c(0.02, 0.2, 0.2)) %>%
      set_bottom_padding(row = 1:7, col= 1:4, 12) %>%
      set_top_padding(row = 1:7, col = 1:4, 12) %>%
      #set_row_height(c(0.02, 0.2, 0.2)) %>%
      set_all_borders(brdr(1, "solid", "black"))
      #set_caption("Pots of jam for sale") # this is if we want a caption - I don't think we need any captions but leaving it here just in case

tab1.1
```

Epipedon:                                                                     Explain:

Diagnostic Subsurface:                                              Explain:

Soil Order:                                                                    Explain:

Soil Suborder:                                                              Explain:

## SOIL 6: ZIMMERMAN

HORIZON CHOICES: Bw2, A, Bw1, C

NOTES: BS = 41%

```{r, echo = FALSE, message = FALSE, warning = FALSE}
library(huxtable)
library(dplyr)

################
## BUILD TABLE
################
Horizon <- c("", "", "", "")
Texture <- c("Loamy Sand", "Loamy Sand", "Loamy Sand", "Loamy Sand")
Color <- c("7.5YR 2.5/1", "7.5YR 6/1", "5YR 3/3", "10YR 6/4")
Depth <- c("0-12.5 cm", "12.5-43 cm", "43-84 cm", "84+ cm")

zimmermanhorizon <- data.frame(Horizon, Texture, Color, Depth)
colnames(zimmermanhorizon) = c("Horizon", "Texture", "Color", "Depth")

col.num = 4
row.num = 5 #including header

################
## SET AS HUXTABLE TYPE
################
zimmermanhorizon_ht <- as_hux(zimmermanhorizon)

################
## SET FORMATTING
################
#NOTE: only thing you really need to mess with down here is the padding. If you need extra row height, keep them - if not, just put a hastag in front of them

tab1.1 = zimmermanhorizon_ht %>% 
      set_all_padding(4) %>% 
      set_outer_padding(0) %>% 
      set_number_format(0) %>% #this tells it how many decimals to print after any numbers
      set_bold(row = 1, col = everywhere) %>% #make header row bold
      #set_bottom_border(row = 1, col = everywhere) %>% # probably dont need this because you set all borders below
      set_width(0.9) %>% #; 1 == full width
      set_align(row = 1:row.num, col = 1:col.num, value = 'center') %>%
      set_valign(row = 1:row.num, col = 1:col.num, value = 'middle') %>%
      #set_number_format(2:3, 1, value = NA) %>% #; don't format these cells as numbers
      #set_col_width(c(0.3, 0.6)) %>%
      set_latex_float(value = "h!") %>%
      #set_row_height(1:3, c(0.02, 0.2, 0.2)) %>%
      set_bottom_padding(row = 1:5, col= 1:4, 12) %>%
      set_top_padding(row = 1:5, col = 1:4, 12) %>%
      #set_row_height(c(0.02, 0.2, 0.2)) %>%
      set_all_borders(brdr(1, "solid", "black"))
      #set_caption("Pots of jam for sale") # this is if we want a caption - I don't think we need any captions but leaving it here just in case

tab1.1
```

Epipedon:                                                                     Explain:

Diagnostic Subsurface:                                              Explain:

Soil Order:                                                                    Explain:

Soil Suborder:                                                              Explain:

## SOIL 7: LESTER

HORIZON CHOICES: EB, Bt2, C, Ap, Bt1

NOTES: BS = 78%, Udic moisture regime

```{r, echo = FALSE, message = FALSE, warning = FALSE}
library(huxtable)
library(dplyr)

################
## BUILD TABLE
################
Horizon <- c("", "", "", "", "")
Texture <- c("Loam", "Loam", "Clay Loam", "Clay Loam", "Loam")
Color <- c("10YR 3/2", "10YR 4/3", "10YR 4/4", "10YR 4/4", "10YR 5/4")
Depth <- c("0-20 cm", "20-31 cm", "31-51 cm", "51-91 cm", "91+ cm")

lesterhorizon <- data.frame(Horizon, Texture, Color, Depth)
colnames(lesterhorizon) = c("Horizon", "Texture", "Color", "Depth")

col.num = 4
row.num = 6 #including header

################
## SET AS HUXTABLE TYPE
################
lesterhorizon_ht <- as_hux(lesterhorizon)

################
## SET FORMATTING
################
#NOTE: only thing you really need to mess with down here is the padding. If you need extra row height, keep them - if not, just put a hastag in front of them

tab1.1 = lesterhorizon_ht %>% 
      set_all_padding(4) %>% 
      set_outer_padding(0) %>% 
      set_number_format(0) %>% #this tells it how many decimals to print after any numbers
      set_bold(row = 1, col = everywhere) %>% #make header row bold
      #set_bottom_border(row = 1, col = everywhere) %>% # probably dont need this because you set all borders below
      set_width(0.9) %>% #; 1 == full width
      set_align(row = 1:row.num, col = 1:col.num, value = 'center') %>%
      set_valign(row = 1:row.num, col = 1:col.num, value = 'middle') %>%
      #set_number_format(2:3, 1, value = NA) %>% #; don't format these cells as numbers
      #set_col_width(c(0.3, 0.6)) %>%
      set_latex_float(value = "h!") %>%
      #set_row_height(1:3, c(0.02, 0.2, 0.2)) %>%
      set_bottom_padding(row = 1:6, col= 1:4, 12) %>%
      set_top_padding(row = 1:6, col = 1:4, 12) %>%
      #set_row_height(c(0.02, 0.2, 0.2)) %>%
      set_all_borders(brdr(1, "solid", "black"))
      #set_caption("Pots of jam for sale") # this is if we want a caption - I don't think we need any captions but leaving it here just in case

tab1.1
```

Epipedon:                                                                     Explain:

Diagnostic Subsurface:                                              Explain:

Soil Order:                                                                    Explain:

Soil Suborder:                                                              Explain:

<br>
<br>
<br>

Note, the suborder for Cecil is not in course handouts - make a guess! Combine the moisture regime and order.

## SOIL 8: CECIL

HORIZON CHOICES: Bt2, A, Bt1, C

NOTES: BS = 18%, Udic moisture regime

```{r, echo = FALSE, message = FALSE, warning = FALSE}
library(huxtable)
library(dplyr)

################
## BUILD TABLE
################
Horizon <- c("", "", "", "")
Texture <- c("Loam", "Clay", "Clay", "Clay Loam")
Color <- c("10YR 4/4", "10YR 4/8", "10YR 4/8", "2.5YR 4/8")
Depth <- c("0-15 cm", "15-41 cm", "41-76 cm", "76+ cm")

cecilhorizon <- data.frame(Horizon, Texture, Color, Depth)
colnames(cecilhorizon) = c("Horizon", "Texture", "Color", "Depth")

col.num = 4
row.num = 5 #including header

################
## SET AS HUXTABLE TYPE
################
cecilhorizon_ht <- as_hux(cecilhorizon)

################
## SET FORMATTING
################
#NOTE: only thing you really need to mess with down here is the padding. If you need extra row height, keep them - if not, just put a hastag in front of them

tab1.1 = cecilhorizon_ht %>% 
      set_all_padding(4) %>% 
      set_outer_padding(0) %>% 
      set_number_format(0) %>% #this tells it how many decimals to print after any numbers
      set_bold(row = 1, col = everywhere) %>% #make header row bold
      #set_bottom_border(row = 1, col = everywhere) %>% # probably dont need this because you set all borders below
      set_width(0.9) %>% #; 1 == full width
      set_align(row = 1:row.num, col = 1:col.num, value = 'center') %>%
      set_valign(row = 1:row.num, col = 1:col.num, value = 'middle') %>%
      #set_number_format(2:3, 1, value = NA) %>% #; don't format these cells as numbers
      #set_col_width(c(0.3, 0.6)) %>%
      set_latex_float(value = "h!") %>%
      #set_row_height(1:3, c(0.02, 0.2, 0.2)) %>%
      set_bottom_padding(row = 1:5, col= 1:4, 12) %>%
      set_top_padding(row = 1:5, col = 1:4, 12) %>%
      #set_row_height(c(0.02, 0.2, 0.2)) %>%
      set_all_borders(brdr(1, "solid", "black"))
      #set_caption("Pots of jam for sale") # this is if we want a caption - I don't think we need any captions but leaving it here just in case

tab1.1
```

Epipedon:                                                                     Explain:

Diagnostic Subsurface:                                              Explain:

Soil Order:                                                                    Explain:

Soil Suborder:                                                              Explain:

## INVESTIGATION B: Location of Soil Orders

Use large map in the lab to label the major soil order (using the numbers below) present at each boxed location in the U.S. There may be more than one location for some Orders and some locations may have more than one dominant Order -- just pick the most obvious one.

![Soil Orders in the USA](usa-soil-orders.png){#fig-usa}

```{r, echo = FALSE, message = FALSE, warning = FALSE}
library(huxtable)
library(dplyr)

################
## BUILD TABLE
################
Alfisols <- c("2. Andisols", "3. Aridisols", "4. Entisols", "5. Gelisols", "6. Histisols")
Inceptisols <- c("8. Mollisols", "9. Oxisols", "10. Spodosols", "11. Ultisols", "12. Vertisols")

alfisolinceptisol <- data.frame(Alfisols, Inceptisols)
colnames(alfisolinceptisol) = c("1. Alfisols", "7. Inceptisols")

col.num = 2
row.num = 6 #including header

################
## SET AS HUXTABLE TYPE
################
alfisolinceptisol_ht <- as_hux(alfisolinceptisol)

################
## SET FORMATTING
################
#NOTE: only thing you really need to mess with down here is the padding. If you need extra row height, keep them - if not, just put a hastag in front of them

tab1.1 = alfisolinceptisol_ht %>% 
      set_all_padding(4) %>% 
      set_outer_padding(0) %>% 
      set_number_format(0) %>% #this tells it how many decimals to print after any numbers
      #set_bold(row = 1, col = everywhere) %>% #make header row bold
      #set_bottom_border(row = 1, col = everywhere) %>% # probably dont need this because you set all borders below
      set_width(0.9) %>% #; 1 == full width
      set_align(row = 1:row.num, col = 1:col.num, value = 'center') %>%
      set_valign(row = 1:row.num, col = 1:col.num, value = 'middle') %>%
      #set_number_format(2:3, 1, value = NA) %>% #; don't format these cells as numbers
      #set_col_width(c(0.3, 0.6)) %>%
      set_latex_float(value = "h!") %>%
      #set_row_height(1:3, c(0.02, 0.2, 0.2)) %>%
      set_bottom_padding(row = 1:6, col= 1:2, 20) %>%
      #set_top_padding(row = 2:3, col = 1:2, 60) %>%
      #set_row_height(c(0.02, 0.2, 0.2)) %>%
      set_all_borders(brdr(0, "solid", "black"))
      #set_caption("Pots of jam for sale") # this is if we want a caption - I don't think we need any captions but leaving it here just in case

tab1.1
```

## INVESTIGATION C: Nomenclature

There are twelve soil orders at the highest hierarchical level of soil taxonomy. The names for these orders relate to Greek, Latin, or other root words that reveal something about the soil. Fifty-four suborders are recognized at the next level of classification. There are over 200 great groups and more than 1,100 subgroups. Soil families have similar physical and chemical properties that affect their response to management. The lowest category -- soil series -- is usually named after a geographic feature in the region in which it was originally found and described. The taxonomic class includes the prefix plus all the following syllables, e.g., a Great Group would be "Haplosaprists", not just the prefix "Haplo-". To help you recognize syllables, remember familiar descriptive terms like sapric, hemic, mesic, ustic, udic, aquic. In addition, look for the prefixes hapl- (simple), orth- (central), epi- (above), argi- (clay) and psamm- (sandy).

Answer the following questions about soil names. #1 is already answered for you.

1.  **(EXAMPLE)** Name the order and suborder for the Okeechobee soil (Euic, hyperthermic, Hemic Haplosaprists).

Order - **Histosol**; Suborder: **Saprist**

a.  List two characteristics of the Okeechobee based on the suborder name.

**Large accumulation of organic materials Sapric organic materials (well decomposed)**

2.  Name the family for the Clarion soil (Fine-loamy, mixed, mesic, Typic Hapludolls).

     

3.  Name the order and suborder for the Fargo soil (Fine, smectitic, frigid, Typic Epiaquerts).

     

3a. Name one characteristic of the Fargo based on the suborder name.

     

3b. Look at the family name -- what property of this soil might you predict based on the properties of the dominant mineral?

     

4.  Name the order and suborder for the Lester soil (Fine-loamy, mixed, mesic, Mollic Hapludalfs).

     

4a. Name one characteristic of the Lester based on the suborder name.

     

5.  Name the order and suborder for the Hubbard soil (Sandy, mixed, frigid, Entic Hapludolls).

     

5a. List two characteristics of the Hubbard based on the suborder name.

     

6.  Name the order and suborder for the Omega soil (Sandy, mixed, frigid, Typic Haplorthods).

     

7.  Name the suborder for the Rifle soil (Euic, frigid, Typic Haplohemists).

     

8.  Name the soil order for the Ves soil (Fine-loamy, mixed, mesic, Calcic Hapludolls).

     

9.  Name the soil order for the Valentine soil (Mixed, mesic, Typic Ustipsamments).

     
